# requirements
1) flask
2) flask_mysqldb


# used mysql database for storing previous records (numbers retrive from the test server (external )api)

DATABASE : AVG_CAL
#TABLES: prime(id,num)
#TABLES: rand(id,num)
#TABLES: even(id,num)
#TABLES: fibo(id,num)


